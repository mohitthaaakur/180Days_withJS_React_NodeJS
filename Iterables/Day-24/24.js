// Chunk iterator
let arrLike = {
  0: 'a',
  1: 'b',
  2: 'c',
  3: 'd',
  length: 4,
};
let arr = Array.from(arrLike, (item, index) => `${item}-${index}`);
console.log(arr);
// Merge two iterables
function zip(iterable1, iterable2){

};
// Infinite iterator + take
